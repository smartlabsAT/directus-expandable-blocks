#-------------------------------------------------------------------------------#
#               Qodana analysis is configured by qodana.yaml file               #
#             https://www.jetbrains.com/help/qodana/qodana-yaml.html            #
#-------------------------------------------------------------------------------#
version: "1.0"

# Specify inspection profile for code analysis
profile:
  name: qodana.recommended

# Fail threshold
failThreshold: 50  # Allow up to 50 issues for now

# Disable specific inspections that might be too strict
exclude:
  - name: SpellCheckingInspection
  - name: GrazieInspection
  - name: JSHint
  - name: StandardJS
  - name: JSUnusedGlobalSymbols
    paths:
      - test
  - name: TypeScriptUnresolvedVariable
    paths:
      - test
  - name: All
    paths:
      - node_modules
      - dist
      - coverage
      - .git
      - test/mocks
      - "*.min.js"
      - .qodana
      - wiki
      - pnpm-lock.yaml
      - package-lock.json

#Execute shell command before Qodana execution (Applied in CI/CD pipeline)
#bootstrap: sh ./prepare-qodana.sh

#Install IDE plugins before Qodana execution (Applied in CI/CD pipeline)
#plugins:
#  - id: <plugin.id> #(plugin id can be found at https://plugins.jetbrains.com)

#Specify Qodana linter for analysis (Applied in CI/CD pipeline)
linter: jetbrains/qodana-js:2023.3
